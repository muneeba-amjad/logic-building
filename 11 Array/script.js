// Beginner Level
// Q1. Write a program to calculate the sum of all numbers in the given array: let num = [2, 4, 6, 7, 9, 10]

// Q2. Find the largest and smallest number in the given array: let numbers = [22, 44, 11, 55, 32, 24, 65, 5]

// Q3. Calculate the average value of numbers in the given array: let numbers = [85, 97, 44, 37, 76, 60]

// Q4. Count the total number of even and odd elements in the given array: let numbers = [102, 93, 144, 35, 56, 107, 78, 99, 103]

// Q5. Reverse the order of elements in the given array without using the built-in .reverse() method: let numbers = [22, 44, 33, 25, 76, 88]

// Q6. Count and store the even and odd elements from the given array into separate arrays, then display the results: let numbers = [102, 93, 144, 35, 56, 107, 78, 99, 103]

// Intermediate Level
// Q1. Write a function to remove all duplicates from the given array: let numbers = [22, 44, 33, 56, 34, 44, 24]

// Q2. Find the second largest number in the given array: let numbers = [22, 44, 34, 23, 45]

// Q3. Sort the given array in ascending order without using the built-in .sort() method: let numbers = [2, 4, 5, 3, 8, 1, 7]

// Q4. Sort the given array in descending order without using the built-in .sort() method: let numbers = [2, 4, 5, 3, 8, 1, 7]

// Q5. For the given array representing student marks, find the average marks of the entire class: let marks = [85, 97, 44, 37, 76, 60]

// Q6. For the given array containing the prices of 5 items, apply a 10% discount offer to each item and update the array to store the final prices: let beforeDiscountPrice = [250, 645, 300, 900, 50]

// Q7. Given the array let arr = ['ram', 'mohan', 'sohan', 'geeta'], write a program to add the new element 'Sita' to the end of it.

// Q8. Given the array let arr = ['ram', 'mohan', 'sohan', 'geeta'], write a program to remove the last element from it.

// Q9. Convert the given array of numbers into a single string: let number = [1, 3, 6, 4, 8]

// Q10. Combine (concatenate) the two given arrays into a single new array: let Name = ['site', 'geeta', 'mohan', 'ram', 'sohan'] and let num = [1, 4, 6, 3, 7]