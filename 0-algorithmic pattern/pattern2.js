// ========================
// PATTERN 2
// Digit Manipulation — "The Scissor & Eraser" Pattern
// ========================

// Use % 10 and Math.trunc(n / 10) to peel numbers apart one digit at a time.
// 6. Count the total number of digits inside a given integer n .
// 7. Find the sum of all digits of a given number (e.g. 123 → 6).
// 8. Find the product of all digits of a given number (e.g. 345 → 60).
// 9. Reverse an integer completely without converting it to a string (e.g. 1234 → 4321).
// 10. Check if a number is a Palindrome (reads the same forwards and backwards).
// 11. Check if a number is an Armstrong Number (sum of digits raised to the power of digit
// count equals the original number).
// 12. Find the largest single digit inside a multi-digit number.
// 13. Find the smallest single digit inside a multi-digit number.
// 14. Count how many times the digit '0' appears inside a number n .
// 15. Extract the first (most significant) digit of any given number. Hint: loop to shrink until n
// < 10.